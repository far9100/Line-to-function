# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/

# generated at the repository root: demo output, evaluation reports, synthetic datasets, the online page
# (anchored with "/" so that tests/data/, the golden digests, stays tracked)
/out/
/runs/
/data/
/_site/
# Pyodide for tests/test_pyodide.py (npm ci --prefix tests/pyodide)
node_modules/

# kept locally, not published: the development log and test drawings (third-party art)
/docs/progress.md
/*.webp
/*.png
/*.jpg
/*.jpeg

# tools and OS files
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
